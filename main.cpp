#include <iostream>
using namespace std;

int main() {

	string str;
    cin>>str;
    string ans;
    if (islower(str[0]) )
    n f ./+c92   0      {

        for(int i=0;i<str.size();i++){
	    ans+=tolower(str[i]);
        }
        cout << ans;
	}  
    else{
        for(int i=0;i<str.size();i++){
	    ans+=toupper(str[i]);
        }
        cout << ans;
    }
    }
